slam_toolbox:
  ros__parameters:
    # General parameters
    map_frame: "map"
    odom_frame: "odom"
    base_frame: "base_link"
    use_sim_time: false
    scan_topic: "/jackal1/sensors/lidar3d_0/scan"
    odom_topic: "/jackal1/platform/odom"
    mode: "mapping"

    # save_map_timeout: 300.0
    # transform_tolerance: 0.1
    # # For saving maps
    # save_map_timeout: 300.0

    # # Localization Parameters
    # map_update_interval: 5.0
    # transform_timeout: 2.0

    # # Path planner
    # planner_frequency: 0.5
    # planner_patience: 5.0
    # planner_max_distance: 10.0

    # # LaserScan Remapping
    # scan_topic: "/jackal1/sensors/lidar3d_0/scan"  # Update this to your actual topic

    # # Map Resolution
    # map_resolution: 0.05  # 5 cm per grid cell

    # # Occupancy grid
    # occupied_thresh: 0.65
    # free_thresh: 0.196
    # mode: trinary
    # origin: [-5.0, -5.0, 0.0]  # This should be adjusted based on your setup
    # # Negate means obstacles are black in the map (0 means white, 1 means black)
    # negate: 0

    # # SLAM-specific parameters
    # publish_frequency: 2.0
    # min_obstacle_height: 0.0
    # max_obstacle_height: 2.0
    # map_update_interval: 5.0
    # transform_tolerance: 0.1
